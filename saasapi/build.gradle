archivesBaseName = 'saasapi'

dependencies {
	compile project(':dao')
	compile project(':core')
	compile(
	)
}

sourceSets {
	main {
		resources {
			exclude '**/static/*'
			exclude 'static'
		}
	}
}

bootRepackage {
	mainClass = 'com.gensoft.Application'
}
