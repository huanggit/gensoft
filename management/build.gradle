archivesBaseName = 'management'

dependencies {
    compile project(':dao')
    compile project(':core')
    compile(
            'org.springframework.boot:spring-boot-starter-velocity'
    )
}

bootRepackage {
    mainClass = 'com.gensoft.Application'
}
